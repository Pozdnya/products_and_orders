import React from 'react';
import { Modal } from '../../Modal';

export const Home = () => {
  return (
    <div style={{ position: 'relative', width: '100%', height: '100%' }}>
      <Modal />
    </div>
  );
};
